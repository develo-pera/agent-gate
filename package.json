{
  "name": "agentgate",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-to-agent DeFi infrastructure — MCP server + Solidity primitives for Lido, MetaMask Delegations, and ENS",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@agentgate/mcp-server && npm run build --workspace=@agentgate/app",
    "start": "cd packages/mcp-server && npm start",
    "dev": "cd packages/app && npm run dev",
    "dev:mcp": "cd packages/mcp-server && npm run dev",
    "test:contracts": "cd packages/treasury-contract && forge test -vvv",
    "rebalance": "npx tsx scripts/rebalance-pools.ts",
    "rebalance:watch": "npx tsx scripts/rebalance-pools.ts --watch"
  },
  "license": "MIT"
}
