import { describe, it, expect } from "vitest";

describe("Playground Tool Selector", () => {
  it.todo("renders all 25 visible tools grouped into 5 domain sections");
  it.todo("shows domain headers: Lido, Treasury, Delegation, ENS, Monitor in order");
  it.todo("collapses and expands domain groups when header is clicked");
  it.todo("filters tools by search query matching name, humanName, or description");
  it.todo("hides domains with zero matching tools during search");
  it.todo("highlights the currently selected tool with accent border");
  it.todo("displays humanName when isHumanView is true, snake_case name when false");
});
