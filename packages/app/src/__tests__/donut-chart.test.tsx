// Coverage: TREA-02 (donut chart renders segments proportionally)
import { describe, test } from "vitest";

describe("DonutChart", () => {
  describe("TREA-02: Vault Composition Donut", () => {
    test.todo("renders principal segment with chart-1 color");
    test.todo("renders yield segment with chart-2 color");
    test.todo("shows total wstETH value in center");
    test.todo("handles zero total with muted ring");
    test.todo("segments are proportional to principal/yield ratio");
  });
});
