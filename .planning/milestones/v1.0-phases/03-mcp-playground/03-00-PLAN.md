---
phase: 03-mcp-playground
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/app/src/__tests__/playground-selector.test.tsx
  - packages/app/src/__tests__/playground-form.test.tsx
  - packages/app/src/__tests__/playground-json.test.tsx
  - packages/app/src/__tests__/playground-dryrun.test.tsx
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04

must_haves:
  truths:
    - "Vitest discovers all 4 playground test stub files and exits 0"
    - "Each stub describes behavioral expectations matching its PLAY requirement"
  artifacts:
    - path: "packages/app/src/__tests__/playground-selector.test.tsx"
      provides: "Test stub for PLAY-01 tool selector behaviors"
      contains: "describe"
    - path: "packages/app/src/__tests__/playground-form.test.tsx"
      provides: "Test stub for PLAY-02 dynamic form behaviors"
      contains: "describe"
    - path: "packages/app/src/__tests__/playground-json.test.tsx"
      provides: "Test stub for PLAY-03 JSON viewer behaviors"
      contains: "describe"
    - path: "packages/app/src/__tests__/playground-dryrun.test.tsx"
      provides: "Test stub for PLAY-04 dry-run toggle behaviors"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 3 playground tasks.

Purpose: VALIDATION.md identifies 4 test files as Wave 0 gaps. Without these stubs, no task in plans 03-01 or 03-02 can satisfy Nyquist automated verification. Vitest infrastructure already exists from Phase 1.
Output: 4 test stub files with .todo() cases, all passing (skipped) under vitest.
</objective>

<execution_context>
@/Users/developera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/developera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mcp-playground/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 playground test stub files</name>
  <files>
    packages/app/src/__tests__/playground-selector.test.tsx
    packages/app/src/__tests__/playground-form.test.tsx
    packages/app/src/__tests__/playground-json.test.tsx
    packages/app/src/__tests__/playground-dryrun.test.tsx
  </files>
  <action>
Create 4 test stub files in `packages/app/src/__tests__/`. Follow the exact pattern from Phase 1 stubs (e.g., `sidebar.test.tsx`): use `describe` + `it.todo()` with behavioral descriptions matching each PLAY requirement.

**`playground-selector.test.tsx`** (PLAY-01 — tool selector listing and filtering):
```typescript
import { describe, it, expect } from "vitest";

describe("Playground Tool Selector", () => {
  it.todo("renders all 25 visible tools grouped into 5 domain sections");
  it.todo("shows domain headers: Lido, Treasury, Delegation, ENS, Monitor in order");
  it.todo("collapses and expands domain groups when header is clicked");
  it.todo("filters tools by search query matching name, humanName, or description");
  it.todo("hides domains with zero matching tools during search");
  it.todo("highlights the currently selected tool with accent border");
  it.todo("displays humanName when isHumanView is true, snake_case name when false");
});
```

**`playground-form.test.tsx`** (PLAY-02 — dynamic parameter form):
```typescript
import { describe, it, expect } from "vitest";

describe("Playground Parameter Form", () => {
  it.todo("renders correct input fields based on selected tool schema params");
  it.todo("renders Select dropdown for enum-type params with correct options");
  it.todo("renders Switch for boolean-type params");
  it.todo("renders number Input for number-type params");
  it.todo("pre-fills smart defaults: wallet address for isAddress params, 1.0 for isAmount params");
  it.todo("shows empty state message when no tool is selected");
  it.todo("shows no-params message for tools with zero parameters");
  it.todo("disables Execute button and shows spinner while isExecuting is true");
  it.todo("marks required params with red asterisk");
});
```

**`playground-json.test.tsx`** (PLAY-03 — JSON viewer):
```typescript
import { describe, it, expect } from "vitest";

describe("Playground JSON Viewer", () => {
  it.todo("renders JSON object with syntax-highlighted keys, strings, numbers, and booleans");
  it.todo("collapses objects deeper than maxInitialDepth");
  it.todo("toggles collapse state when clicking object/array bracket");
  it.todo("shows item count on collapsed nodes");
  it.todo("copies JSON to clipboard when copy button is clicked");
  it.todo("triggers download of .json file when download button is clicked");
  it.todo("toggles word wrap on the code container");
});
```

**`playground-dryrun.test.tsx`** (PLAY-04 — dry-run toggle):
```typescript
import { describe, it, expect } from "vitest";

describe("Playground Dry-Run Toggle", () => {
  it.todo("injects dry_run=true into request body when globalDryRun is enabled for write tools");
  it.todo("does not inject dry_run for read-only tools regardless of toggle state");
  it.todo("forces dry_run=true in demo mode even if toggle is off");
  it.todo("status bar shows Dry Run badge when execution was dry-run");
  it.todo("global dry-run defaults to true on initial load");
});
```
  </action>
  <verify>
    <automated>cd /Users/developera/hackathons/synthesis/agent-gate/packages/app && npx vitest run --reporter=verbose 2>&1 | grep -E "(playground-|Tests|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>All 4 playground test stubs exist in packages/app/src/__tests__/, vitest discovers them, exits 0 with all tests listed as todo/skipped.</done>
</task>

</tasks>

<verification>
1. `cd packages/app && npx vitest run --reporter=verbose` exits 0
2. All 4 playground test files are discovered by Vitest
3. Each file has behavioral .todo() tests matching its PLAY requirement
</verification>

<success_criteria>
- 4 test stub files exist with .todo() cases
- Vitest runs with zero failures (todo tests are skipped, not failing)
- Wave 0 gap from VALIDATION.md is fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-mcp-playground/03-00-SUMMARY.md`
</output>
