---
phase: 04-foundation-verification-config-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - .env.example
  - packages/app/src/lib/hooks/use-delegations.ts
  - packages/mcp-server/src/bridge.ts
  - .planning/phases/01-foundation/01-VERIFICATION.md
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-04
  - FOUN-05
  - FOUN-06
must_haves:
  truths:
    - "NEXT_PUBLIC_TREASURY_ADDRESS env var is defined and wagmi hooks resolve the real treasury address instead of zero address"
    - "useDelegationActions function no longer exists in use-delegations.ts"
    - "getAvailableTools function no longer exists in bridge.ts"
    - "Phase 1 VERIFICATION.md exists with PASS/FAIL evidence for all 6 FOUN-* requirements"
  artifacts:
    - path: ".env"
      provides: "NEXT_PUBLIC_TREASURY_ADDRESS env var"
      contains: "NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9"
    - path: ".env.example"
      provides: "NEXT_PUBLIC_TREASURY_ADDRESS env var template"
      contains: "NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9"
    - path: "packages/app/src/lib/hooks/use-delegations.ts"
      provides: "Delegations hook without dead useDelegationActions"
      exports: ["Delegation", "useDelegations"]
    - path: "packages/mcp-server/src/bridge.ts"
      provides: "Bridge without dead getAvailableTools"
    - path: ".planning/phases/01-foundation/01-VERIFICATION.md"
      provides: "Phase 1 verification report"
      contains: "FOUN-01"
  key_links:
    - from: "packages/app/src/lib/contracts/addresses.ts"
      to: ".env"
      via: "process.env.NEXT_PUBLIC_TREASURY_ADDRESS"
      pattern: "NEXT_PUBLIC_TREASURY_ADDRESS"
---

<objective>
Fix the NEXT_PUBLIC_TREASURY_ADDRESS env var gap so wagmi reads resolve the real treasury address, remove 2 dead code exports (useDelegationActions, getAvailableTools), and create the missing Phase 1 VERIFICATION.md with evidence-based verification of all 6 FOUN-* requirements.

Purpose: Close all code-level and verification audit gaps from the v1.0 milestone audit.
Output: Working env var, clean codebase, Phase 1 verification report.
</objective>

<execution_context>
@/Users/developera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/developera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-foundation-verification-config-fix/04-CONTEXT.md
@.planning/phases/02-dashboard-pages/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NEXT_PUBLIC_TREASURY_ADDRESS env var and remove dead code</name>
  <files>.env, .env.example, packages/app/src/lib/hooks/use-delegations.ts, packages/mcp-server/src/bridge.ts</files>
  <read_first>
    - .env (current env vars — need to see existing content to add new var in correct section)
    - .env.example (current template — need to see structure for consistent placement)
    - packages/app/src/lib/contracts/addresses.ts (reads NEXT_PUBLIC_TREASURY_ADDRESS — confirms the env var name)
    - packages/app/src/lib/hooks/use-delegations.ts (full file — need to see what to keep vs remove)
    - packages/mcp-server/src/bridge.ts (lines 460-468 — getAvailableTools function to remove)
  </read_first>
  <action>
**Env var fix — .env:**
Add the following line after the existing `TREASURY_ADDRESS` line (line 6):
```
NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9
```
This is the same value as TREASURY_ADDRESS. The NEXT_PUBLIC_ prefix makes it available to client-side Next.js code. Without it, `addresses.ts` line 3 falls back to the zero address and all wagmi hooks query a nonexistent contract.

**Env var fix — .env.example:**
Add the following line in the `# -- Contracts` section, after the existing `TREASURY_ADDRESS` line (line 16):
```
NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9
```
Use the real address (not a placeholder) so cloners get a working demo immediately, per user decision.

**Dead code removal — use-delegations.ts:**
Remove the entire `useDelegationActions` function (lines 47-68). Keep everything else intact:
- Lines 1-4: imports (remove `useCallback` from the import since only useDelegationActions uses it)
- Lines 6-13: Delegation interface (KEEP)
- Lines 15-32: DEMO_DELEGATIONS constant (KEEP)
- Lines 34-45: useDelegations function (KEEP)
- Lines 47-68: useDelegationActions function (DELETE)

After removal, the import line should be:
```typescript
import { useState } from "react";
```
(Remove `useCallback` since it is no longer used.)

**Dead code removal — bridge.ts:**
Remove lines 463-467 (the `// -- Utility` comment and the `getAvailableTools` function):
```
// -- Utility -----...
export function getAvailableTools(): string[] {
  return Object.keys(toolRegistry);
}
```
Leave the closing `};` of the toolRegistry object (line 461) and the blank line after it. The file should end after line 462 (the blank line after the toolRegistry closing brace).
  </action>
  <verify>
    <automated>cd /Users/developera/hackathons/synthesis/agent-gate && grep -q "NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9" .env && grep -q "NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9" .env.example && ! grep -q "useDelegationActions" packages/app/src/lib/hooks/use-delegations.ts && ! grep -q "getAvailableTools" packages/mcp-server/src/bridge.ts && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .env contains line: NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9
    - .env.example contains line: NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9
    - use-delegations.ts does NOT contain the string "useDelegationActions"
    - use-delegations.ts does NOT contain the string "useCallback"
    - use-delegations.ts still contains "export function useDelegations()"
    - use-delegations.ts still contains "export interface Delegation"
    - bridge.ts does NOT contain the string "getAvailableTools"
    - bridge.ts still contains "export const toolRegistry"
    - `npx tsc --noEmit` in packages/app exits 0 (no type errors from removed exports)
  </acceptance_criteria>
  <done>NEXT_PUBLIC_TREASURY_ADDRESS is set in both .env and .env.example with value 0xb1C79423C959b33e7353693D795DA417575A6bf9. useDelegationActions and getAvailableTools are fully removed. No TypeScript compilation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 1 VERIFICATION.md with evidence for all FOUN-* requirements</name>
  <files>.planning/phases/01-foundation/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-dashboard-pages/02-VERIFICATION.md (format reference — replicate this structure exactly)
    - .planning/REQUIREMENTS.md (FOUN-01 through FOUN-06 definitions — these are the requirements to verify)
    - .planning/v1.0-MILESTONE-AUDIT.md (audit gaps — this is what we are closing)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (what Phase 1 Plan 01 built)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (what Phase 1 Plan 02 built)
    - packages/app/package.json (confirms Next.js workspace package exists — FOUN-01)
    - packages/app/tailwind.config.ts (confirms Tailwind CSS config — FOUN-01)
    - packages/app/src/app/globals.css (confirms dark theme CSS — FOUN-02)
    - packages/app/src/app/layout.tsx (confirms dark class on html — FOUN-02)
    - packages/app/src/providers/wallet-provider.tsx (confirms RainbowKit — FOUN-03)
    - packages/app/src/providers/app-provider.tsx (confirms demo mode — FOUN-04)
    - packages/app/src/app/api/mcp/[tool]/route.ts (confirms HTTP bridge — FOUN-05)
    - packages/app/src/components/layout/sidebar.tsx (confirms navigation — FOUN-06)
  </read_first>
  <action>
Create `.planning/phases/01-foundation/01-VERIFICATION.md` following the exact format of `02-VERIFICATION.md`. The file must contain:

**Frontmatter:**
```yaml
---
phase: 01-foundation
verified: 2026-03-20T00:00:00Z
status: passed
score: "6/6 must-haves verified"
re_verification: true
human_verification:
  - test: "Dark crypto theme renders with forced dark mode and glowing accents"
    expected: "App shows dark backgrounds (#131313), hot-pink primary (#FF37C7), no light mode flicker"
    why_human: "CSS custom properties and forced dark mode require browser rendering to confirm"
  - test: "RainbowKit wallet connect modal appears and MetaMask option is available"
    expected: "Connect Wallet button opens RainbowKit modal with MetaMask and WalletConnect options; Base network badge visible after connect"
    why_human: "RainbowKit modal rendering and MetaMask detection require browser with extension"
  - test: "Demo mode activates when no wallet is connected"
    expected: "App loads without wallet and shows full navigation with read-only data; isDemo=true in AppProvider"
    why_human: "Requires running app in browser without wallet connection to confirm demo behavior"
---
```

**Sections (replicate 02-VERIFICATION.md structure):**

1. **Header:** `# Phase 01: Foundation Verification Report`
2. **Meta:** Phase Goal, Verified date, Status: PASSED, Re-verification: Yes (retroactive)

3. **Goal Achievement > Observable Truths table:**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Next.js app exists as npm workspace package with Tailwind CSS | VERIFIED | `packages/app/package.json` exists with `next` dependency; `tailwind.config.ts` present; `postcss.config.mjs` configured |
| 2 | Dark crypto theme applied globally with forced dark mode | VERIFIED | `layout.tsx` has `className="dark"` on html element; `globals.css` defines CSS custom properties for dark theme; `--background` and `--primary` set to dark values |
| 3 | Wallet connect via RainbowKit with MetaMask support | VERIFIED | `wallet-provider.tsx` imports RainbowKitProvider, configures Base chain, includes metaMaskWallet in wallet list |
| 4 | Demo mode — app fully explorable without wallet connection | VERIFIED | `app-provider.tsx` exports `useApp()` with `isDemo` derived from wallet connection state; pages render with demo data when `isDemo=true` |
| 5 | HTTP bridge at /api/mcp/[tool] accepts POST and returns JSON from MCP handlers | VERIFIED | `packages/app/src/app/api/mcp/[tool]/route.ts` exports POST handler; imports `toolRegistry` from bridge; dispatches to handler by tool name |
| 6 | App shell with sidebar navigation linking to all dashboard sections | VERIFIED | `sidebar.tsx` renders nav links to `/treasury`, `/staking`, `/delegations`, `/playground`; CSS group-hover expand/collapse |

4. **Required Artifacts table:** List all key Phase 1 files with VERIFIED status. Run actual grep/file-exists checks during execution to fill the evidence column with real line numbers and export names.

5. **Key Link Verification table:** Verify these links:
   - wallet-provider.tsx -> RainbowKitProvider (import)
   - app-provider.tsx -> useAccount (wallet state for isDemo)
   - api/mcp/[tool]/route.ts -> bridge.ts toolRegistry (import)
   - sidebar.tsx -> Next.js Link to /treasury, /staking, /delegations, /playground
   - layout.tsx -> globals.css (dark theme application)

6. **Requirements Coverage table:** Map each FOUN-01 through FOUN-06 to SATISFIED status with evidence.

7. **Anti-Patterns Found:** Note any issues found during verification (likely none for Phase 1 core).

8. **Human Verification Required:** 3 items from frontmatter (dark theme visual, RainbowKit modal, demo mode behavior).

9. **Gaps Summary:** "None. All automated verification checks passed." (or document any issues found).

**IMPORTANT:** During execution, actually read each source file and grep for the specific patterns. Do not fabricate line numbers or evidence. Every evidence field must reference real content found in the actual files.
  </action>
  <verify>
    <automated>test -f /Users/developera/hackathons/synthesis/agent-gate/.planning/phases/01-foundation/01-VERIFICATION.md && grep -q "FOUN-01" /Users/developera/hackathons/synthesis/agent-gate/.planning/phases/01-foundation/01-VERIFICATION.md && grep -q "FOUN-06" /Users/developera/hackathons/synthesis/agent-gate/.planning/phases/01-foundation/01-VERIFICATION.md && grep -q "status: passed" /Users/developera/hackathons/synthesis/agent-gate/.planning/phases/01-foundation/01-VERIFICATION.md && echo "VERIFICATION EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/01-foundation/01-VERIFICATION.md exists
    - Frontmatter contains `phase: 01-foundation`
    - Frontmatter contains `status: passed`
    - Frontmatter contains `score:` field with "6/6"
    - File contains all 6 requirement IDs: FOUN-01, FOUN-02, FOUN-03, FOUN-04, FOUN-05, FOUN-06
    - Each FOUN-* requirement has SATISFIED or VERIFIED status with non-empty evidence
    - File contains "Observable Truths" section with a table
    - File contains "Requirements Coverage" section with a table
    - File contains "Human Verification Required" section
    - File follows the same markdown structure as 02-VERIFICATION.md (frontmatter, goal achievement, artifacts, key links, requirements, anti-patterns, human verification, gaps summary)
  </acceptance_criteria>
  <done>Phase 1 VERIFICATION.md exists with evidence-based PASS results for all 6 FOUN-* requirements, following the exact format of Phase 2's verification report. Audit gap "Phase 1 has no VERIFICATION.md" is closed.</done>
</task>

</tasks>

<verification>
- `grep -q "NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9" .env` exits 0
- `grep -q "NEXT_PUBLIC_TREASURY_ADDRESS=0xb1C79423C959b33e7353693D795DA417575A6bf9" .env.example` exits 0
- `! grep -q "useDelegationActions" packages/app/src/lib/hooks/use-delegations.ts` exits 0
- `! grep -q "getAvailableTools" packages/mcp-server/src/bridge.ts` exits 0
- `test -f .planning/phases/01-foundation/01-VERIFICATION.md` exits 0
- All 6 FOUN-* IDs appear in 01-VERIFICATION.md
</verification>

<success_criteria>
- NEXT_PUBLIC_TREASURY_ADDRESS env var set in .env and .env.example with real treasury address
- Dead code (useDelegationActions, getAvailableTools) fully removed with no TypeScript errors
- Phase 1 VERIFICATION.md created with evidence-based verification for FOUN-01 through FOUN-06
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation-verification-config-fix/04-01-SUMMARY.md`
</output>
