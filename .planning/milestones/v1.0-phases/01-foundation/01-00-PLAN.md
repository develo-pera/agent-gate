---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/app/vitest.config.ts
  - packages/app/src/__tests__/theme.test.ts
  - packages/app/src/__tests__/providers.test.tsx
  - packages/app/src/__tests__/demo-mode.test.tsx
  - packages/app/src/__tests__/api-bridge.test.ts
  - packages/app/src/__tests__/sidebar.test.tsx
autonomous: true
requirements:
  - FOUN-01

must_haves:
  truths:
    - "Vitest runs successfully in packages/app with zero tests failing"
    - "All 6 test stub files exist and are importable"
  artifacts:
    - path: "packages/app/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment"
      contains: "jsdom"
    - path: "packages/app/src/__tests__/theme.test.ts"
      provides: "CSS variable validation test stub"
      contains: "describe"
    - path: "packages/app/src/__tests__/providers.test.tsx"
      provides: "Provider tree render test stub"
      contains: "describe"
    - path: "packages/app/src/__tests__/demo-mode.test.tsx"
      provides: "Demo mode context logic test stub"
      contains: "describe"
    - path: "packages/app/src/__tests__/api-bridge.test.ts"
      provides: "Bridge route handler test stub"
      contains: "describe"
    - path: "packages/app/src/__tests__/sidebar.test.tsx"
      provides: "Sidebar navigation render test stub"
      contains: "describe"
  key_links: []
---

<objective>
Install test infrastructure and create all Wave 0 test stub files for Phase 1.

Purpose: Establishes the Vitest test framework and creates skeleton test files that Wave 1+ tasks will flesh out. Without this, no plan can satisfy the Nyquist validation contract.
Output: Vitest config + 6 test stub files, all passing (empty or trivial assertions).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create test infrastructure with all stub files</name>
  <files>
    packages/app/vitest.config.ts,
    packages/app/src/__tests__/theme.test.ts,
    packages/app/src/__tests__/providers.test.tsx,
    packages/app/src/__tests__/demo-mode.test.tsx,
    packages/app/src/__tests__/api-bridge.test.ts,
    packages/app/src/__tests__/sidebar.test.tsx
  </files>
  <action>
1. Install test dependencies in the app package:
   ```bash
   cd packages/app && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react
   ```

2. Create `packages/app/vitest.config.ts`:
   ```typescript
   import { defineConfig } from "vitest/config";
   import react from "@vitejs/plugin-react";
   import path from "path";

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: "jsdom",
       setupFiles: [],
       globals: true,
     },
     resolve: {
       alias: {
         "@": path.resolve(__dirname, "./src"),
       },
     },
   });
   ```

3. Add test script to `packages/app/package.json`:
   ```json
   "test": "vitest run --reporter=verbose"
   ```

4. Create `packages/app/src/__tests__/` directory and all 6 stub files. Each stub should have a single passing test so Vitest confirms the file is loadable:

   `theme.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("Theme CSS Variables", () => {
     it.todo("validates --background CSS variable is set to dark theme value");
     it.todo("validates --primary CSS variable uses accent purple");
     it.todo("validates --success and --warning CSS variables exist");
   });
   ```

   `providers.test.tsx`:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("Provider Tree", () => {
     it.todo("renders Web3Provider without crashing");
     it.todo("renders AppProvider with default demo context");
   });
   ```

   `demo-mode.test.tsx`:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("Demo Mode", () => {
     it.todo("defaults to demo mode when no wallet connected");
     it.todo("uses DEMO_TREASURY_ADDRESS as activeAddress in demo mode");
     it.todo("switches to connected mode when wallet is connected");
   });
   ```

   `api-bridge.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("MCP API Bridge", () => {
     it.todo("returns 404 for unknown tool names");
     it.todo("returns { success, data } shape for valid tool calls");
     it.todo("creates bridge context with correct defaults");
   });
   ```

   `sidebar.test.tsx`:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("Sidebar Navigation", () => {
     it.todo("renders 4 navigation items: Treasury, Staking, Delegations, Playground");
     it.todo("highlights active route with accent border");
     it.todo("shows AgentGate wordmark when expanded");
   });
   ```
  </action>
  <verify>
    <automated>cd packages/app && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Vitest installed and configured with jsdom environment. All 6 test stub files exist in packages/app/src/__tests__/ with .todo() tests that Vitest recognizes. `npx vitest run` exits 0 with all stubs listed as skipped/todo.</done>
</task>

</tasks>

<verification>
1. `cd packages/app && npx vitest run --reporter=verbose` exits 0
2. All 6 test files are discovered by Vitest
3. vitest.config.ts uses jsdom environment
4. packages/app/package.json has "test" script
</verification>

<success_criteria>
- Vitest runs in packages/app with zero failures
- 6 test stub files exist with .todo() test cases describing expected behaviors
- vitest.config.ts configured with jsdom, React plugin, and @ alias
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
