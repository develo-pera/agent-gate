---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/app/src/providers/web3-provider.tsx
  - packages/app/src/providers/app-provider.tsx
  - packages/app/src/lib/wagmi-config.ts
  - packages/app/src/components/sidebar.tsx
  - packages/app/src/components/demo-banner.tsx
  - packages/app/src/components/wallet-display.tsx
  - packages/app/src/components/placeholder-page.tsx
  - packages/app/src/app/layout.tsx
  - packages/app/src/app/page.tsx
  - packages/app/src/app/treasury/page.tsx
  - packages/app/src/app/staking/page.tsx
  - packages/app/src/app/delegations/page.tsx
  - packages/app/src/app/playground/page.tsx
autonomous: false
requirements:
  - FOUN-03
  - FOUN-04
  - FOUN-06

must_haves:
  truths:
    - "The app renders a dark crypto-themed UI with sidebar navigation linking to Treasury, Staking, Delegations, and Playground sections"
    - "User can connect a MetaMask wallet via RainbowKit and see their address displayed with a Base network badge"
    - "User can browse the entire app in demo mode without connecting a wallet, seeing the demo banner with treasury address"
    - "Sidebar is a narrow icon rail that expands on hover to show labels"
  artifacts:
    - path: "packages/app/src/providers/web3-provider.tsx"
      provides: "WagmiProvider + RainbowKitProvider + QueryClientProvider stack"
      contains: "RainbowKitProvider"
    - path: "packages/app/src/providers/app-provider.tsx"
      provides: "Demo mode context with active address"
      exports: ["AppProvider", "useApp"]
    - path: "packages/app/src/components/sidebar.tsx"
      provides: "Icon rail sidebar with hover expand and 4 nav items"
      contains: "Treasury"
    - path: "packages/app/src/components/demo-banner.tsx"
      provides: "Demo mode banner with treasury address"
      contains: "Demo Mode"
    - path: "packages/app/src/components/wallet-display.tsx"
      provides: "Connected wallet address + Base badge"
      contains: "Base"
    - path: "packages/app/src/lib/wagmi-config.ts"
      provides: "wagmi config for Base chain with RainbowKit"
      contains: "getDefaultConfig"
    - path: "packages/app/src/app/treasury/page.tsx"
      provides: "Treasury placeholder page"
      contains: "Treasury"
    - path: "packages/app/src/app/staking/page.tsx"
      provides: "Staking placeholder page"
      contains: "Staking"
    - path: "packages/app/src/app/delegations/page.tsx"
      provides: "Delegations placeholder page"
      contains: "Delegations"
    - path: "packages/app/src/app/playground/page.tsx"
      provides: "Playground placeholder page"
      contains: "Playground"
  key_links:
    - from: "packages/app/src/app/layout.tsx"
      to: "packages/app/src/providers/web3-provider.tsx"
      via: "Web3Provider wraps children"
      pattern: "Web3Provider"
    - from: "packages/app/src/app/layout.tsx"
      to: "packages/app/src/providers/app-provider.tsx"
      via: "AppProvider wraps children inside Web3Provider"
      pattern: "AppProvider"
    - from: "packages/app/src/app/layout.tsx"
      to: "packages/app/src/components/sidebar.tsx"
      via: "Sidebar rendered in layout alongside main content"
      pattern: "Sidebar"
    - from: "packages/app/src/app/layout.tsx"
      to: "packages/app/src/components/demo-banner.tsx"
      via: "DemoBanner rendered at top of layout"
      pattern: "DemoBanner"
    - from: "packages/app/src/providers/app-provider.tsx"
      to: "packages/app/src/lib/constants.ts"
      via: "imports DEMO_TREASURY_ADDRESS"
      pattern: "DEMO_TREASURY_ADDRESS"
    - from: "packages/app/src/components/sidebar.tsx"
      to: "packages/app/src/components/wallet-display.tsx"
      via: "WalletDisplay rendered in sidebar bottom section"
      pattern: "WalletDisplay"

user_setup:
  - service: walletconnect
    why: "RainbowKit requires WalletConnect project ID for wallet connections"
    env_vars:
      - name: NEXT_PUBLIC_WC_PROJECT_ID
        source: "https://cloud.walletconnect.com -- create free project, copy Project ID"
    dashboard_config:
      - task: "Create a new project"
        location: "cloud.walletconnect.com -> New Project -> copy Project ID"
---

<objective>
Wire up wallet connect, demo mode, sidebar navigation, and all placeholder pages.

Purpose: Completes the Phase 1 app shell -- a fully navigable, polished dark UI with RainbowKit wallet connect, demo mode banner, and four placeholder sections. This is what the user sees and interacts with.
Output: Complete app shell with working navigation, wallet connection, and demo mode.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@packages/app/src/app/layout.tsx
@packages/app/src/app/globals.css
@packages/app/src/lib/constants.ts
@packages/app/src/lib/utils.ts

<interfaces>
<!-- From Plan 01 outputs the executor needs -->

From packages/app/src/lib/constants.ts:
```typescript
export const DEMO_TREASURY_ADDRESS: string;
export const APP_NAME: string;
```

From packages/app/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

shadcn components available (from Plan 01):
- button, avatar, badge, tooltip, separator, skeleton
- Import from "@/components/ui/{component}"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create providers, wagmi config, wallet display, and demo mode</name>
  <files>
    packages/app/src/lib/wagmi-config.ts,
    packages/app/src/providers/web3-provider.tsx,
    packages/app/src/providers/app-provider.tsx,
    packages/app/src/components/wallet-display.tsx,
    packages/app/src/components/demo-banner.tsx
  </files>
  <read_first>
    packages/app/src/app/layout.tsx,
    packages/app/src/lib/constants.ts,
    packages/app/src/app/globals.css,
    .planning/phases/01-foundation/01-UI-SPEC.md,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
1. Install RainbowKit + wagmi + viem + react-query in the app package:
   ```bash
   cd packages/app && npm install @rainbow-me/rainbowkit wagmi viem @tanstack/react-query lucide-react
   ```

2. Create `packages/app/src/lib/wagmi-config.ts`:
   ```typescript
   import { getDefaultConfig } from "@rainbow-me/rainbowkit";
   import { base } from "wagmi/chains";

   export const wagmiConfig = getDefaultConfig({
     appName: "AgentGate",
     projectId: process.env.NEXT_PUBLIC_WC_PROJECT_ID || "demo",
     chains: [base],
     ssr: true,
   });
   ```

3. Create `packages/app/src/providers/web3-provider.tsx` (must be `"use client"`):
   - Import and use `RainbowKitProvider` with `darkTheme({ accentColor: "#8B5CF6", borderRadius: "medium" })`
   - Wrap with `WagmiProvider` using `wagmiConfig`
   - Wrap with `QueryClientProvider` using a `new QueryClient()` created OUTSIDE the component body (module-level const)
   - Import `@rainbow-me/rainbowkit/styles.css`

4. Create `packages/app/src/providers/app-provider.tsx` (must be `"use client"`):
   - Import `useAccount` from `wagmi`
   - Import `DEMO_TREASURY_ADDRESS` from `@/lib/constants`
   - Define and export `AppContextValue` interface: `{ isDemo: boolean; activeAddress: string; treasuryAddress: string; }`
   - Create React context with default value `{ isDemo: true, activeAddress: DEMO_TREASURY_ADDRESS, treasuryAddress: DEMO_TREASURY_ADDRESS }`
   - `AppProvider` component uses `useAccount()` to get `{ address, isConnected }`
   - When `isConnected && address`, set `isDemo: false`, `activeAddress: address`
   - Otherwise set `isDemo: true`, `activeAddress: DEMO_TREASURY_ADDRESS`
   - Export `useApp` hook: `() => useContext(AppContext)`

5. Create `packages/app/src/components/demo-banner.tsx` (must be `"use client"`):
   - Import `useApp` from `@/providers/app-provider`
   - Import `ConnectButton` from `@rainbow-me/rainbowkit`
   - If `!isDemo`, return `null` (banner disappears when wallet connected)
   - Render a sticky top bar, 40px height, full width
   - Background: `hsl(38 92% 50% / 0.1)` (warning tint from UI-SPEC)
   - Bottom border: `1px solid hsl(38 92% 50% / 0.3)`
   - Layout: centered text + right-aligned CTA
   - Text: 14px regular, muted-foreground color, content: `"Demo Mode -- viewing "` + truncated treasury address (first 6 + last 4 chars, e.g., `0x1234...abcd`), mono font for address portion
   - CTA: Use a ghost button variant with accent text color, text "Connect Wallet", that triggers RainbowKit connect (use `ConnectButton.Custom` for just a trigger, or place a small `ConnectButton` variant)
   - Add `role="status"` on the banner container per UI-SPEC accessibility spec

6. Create `packages/app/src/components/wallet-display.tsx` (must be `"use client"`):
   - Import `useApp` from `@/providers/app-provider`
   - Import `useEnsName` from `wagmi` and `ConnectButton` from `@rainbow-me/rainbowkit`
   - Import `Badge` from `@/components/ui/badge` and `Button` from `@/components/ui/button`
   - When disconnected (`isDemo`): render a `Button` variant="default" (accent/primary) with text "Connect Wallet" that triggers RainbowKit connect modal
   - When connected: render the user's ENS name (if available via `useEnsName`) or truncated address (first 6 + last 4, mono font 13px), plus a `Badge` with text "Base" in accent variant
   - Use `ConnectButton.Custom` to get the `openConnectModal` function for the button onClick
   - Address text uses `font-mono text-[13px]` per UI-SPEC
  </action>
  <verify>
    <automated>cd packages/app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - packages/app/src/lib/wagmi-config.ts contains "getDefaultConfig"
    - packages/app/src/lib/wagmi-config.ts contains "ssr: true"
    - packages/app/src/lib/wagmi-config.ts contains "base" (chain import)
    - packages/app/src/providers/web3-provider.tsx contains '"use client"'
    - packages/app/src/providers/web3-provider.tsx contains "RainbowKitProvider"
    - packages/app/src/providers/web3-provider.tsx contains "darkTheme"
    - packages/app/src/providers/web3-provider.tsx contains "#8B5CF6"
    - packages/app/src/providers/web3-provider.tsx contains "QueryClientProvider"
    - packages/app/src/providers/web3-provider.tsx contains "@rainbow-me/rainbowkit/styles.css"
    - packages/app/src/providers/app-provider.tsx contains '"use client"'
    - packages/app/src/providers/app-provider.tsx contains "useAccount"
    - packages/app/src/providers/app-provider.tsx contains "DEMO_TREASURY_ADDRESS"
    - packages/app/src/providers/app-provider.tsx contains "export const useApp"
    - packages/app/src/providers/app-provider.tsx contains "isDemo"
    - packages/app/src/components/demo-banner.tsx contains "Demo Mode"
    - packages/app/src/components/demo-banner.tsx contains 'role="status"'
    - packages/app/src/components/demo-banner.tsx contains "useApp"
    - packages/app/src/components/wallet-display.tsx contains "Connect Wallet"
    - packages/app/src/components/wallet-display.tsx contains "Base"
    - packages/app/src/components/wallet-display.tsx contains "font-mono"
    - `cd packages/app && npm run build` exits 0
  </acceptance_criteria>
  <done>Web3 provider stack (wagmi + RainbowKit + react-query) renders without errors. Demo mode context tracks wallet connection and provides correct activeAddress. Demo banner shows "Demo Mode -- viewing 0x0000...0000" and disappears when wallet connected. Wallet display shows "Connect Wallet" button or connected address with Base badge.</done>
</task>

<task type="auto">
  <name>Task 2: Build sidebar navigation, placeholder pages, and wire layout</name>
  <files>
    packages/app/src/components/sidebar.tsx,
    packages/app/src/components/placeholder-page.tsx,
    packages/app/src/app/layout.tsx,
    packages/app/src/app/page.tsx,
    packages/app/src/app/treasury/page.tsx,
    packages/app/src/app/staking/page.tsx,
    packages/app/src/app/delegations/page.tsx,
    packages/app/src/app/playground/page.tsx
  </files>
  <read_first>
    packages/app/src/app/layout.tsx,
    packages/app/src/app/globals.css,
    packages/app/src/providers/web3-provider.tsx,
    packages/app/src/providers/app-provider.tsx,
    packages/app/src/components/wallet-display.tsx,
    packages/app/src/components/demo-banner.tsx,
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
1. Create `packages/app/src/components/placeholder-page.tsx`:
   - Accepts props: `{ icon: LucideIcon; title: string; subtitle?: string }`
   - Renders a centered glassmorphism card, max-width 480px
   - Card styles: `rounded-xl border border-border/50 bg-card/60 backdrop-blur-lg p-8 shadow-[0_0_24px_hsl(270_95%_65%/0.15)]`
   - Icon: 48px, `text-muted-foreground`
   - Title: `text-xl font-semibold text-foreground` (20px / 600 per UI-SPEC)
   - Subtitle: `text-sm text-muted-foreground` (14px / 400), defaults to "Coming soon"
   - Vertical stack layout, centered text, 16px gap (`space-y-4` or `gap-4`)

2. Create `packages/app/src/components/sidebar.tsx` (`"use client"`):

   Navigation items (hardcoded array):
   ```typescript
   const NAV_ITEMS = [
     { href: "/treasury", label: "Treasury", icon: Vault },
     { href: "/staking", label: "Staking", icon: TrendingUp },
     { href: "/delegations", label: "Delegations", icon: Users },
     { href: "/playground", label: "Playground", icon: Terminal },
   ];
   ```
   Import icons from `lucide-react`.

   Structure:
   - Fixed left, full viewport height (`fixed left-0 top-0 h-screen`)
   - Background `bg-card`, 1px right border `border-r border-border`
   - Default width: 56px. On hover/focus-within: 240px
   - Use Tailwind `group` class on the sidebar container
   - Width transition: `w-14 group-hover:w-60 focus-within:w-60 transition-all duration-200`
   - Add `tabIndex={0}` to sidebar container or use `<nav>` to enable focus-within

   Logo section (top):
   - "AgentGate" wordmark at 28px / 600 (Display size), visible when expanded
   - Collapsed: show "AG" monogram at same size
   - Use `overflow-hidden whitespace-nowrap` to handle text truncation during transition
   - Accent glow: `text-shadow: 0 0 20px hsl(270 95% 65% / 0.5)` via inline style or custom utility
   - Apply accent color to the text (`text-primary`)

   Nav items:
   - Use `usePathname()` from `next/navigation` for active route detection
   - Each item is a `<Link>` from `next/link`
   - Container: 44px min height touch target, flex items-center, px-4 gap-3
   - Active state: 3px left border in accent color (`border-l-[3px] border-primary`), icon in accent (`text-primary`), label in foreground
   - Inactive state: no left border (use `border-l-[3px] border-transparent` for alignment), icon in `text-muted-foreground`, label in `text-muted-foreground`
   - Hover state: `hover:bg-muted`, icon transitions to foreground color
   - Icon size: 20px (`w-5 h-5`), flex-shrink-0
   - Label: 14px / 600 (`text-sm font-semibold`), hidden when collapsed, visible when expanded. Use `opacity-0 group-hover:opacity-100` with transition, or `hidden group-hover:inline`

   Wallet section (bottom):
   - Separated by a `Separator` component from shadcn
   - Render `<WalletDisplay />` component
   - Positioned at bottom via flex layout: sidebar uses `flex flex-col`, nav gets `flex-1`, wallet section at end

   ARIA: `<nav aria-label="Main navigation">` wrapper around the nav items list

   Focus ring on nav items: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background`

3. Create placeholder pages -- each is a simple server component:

   `packages/app/src/app/treasury/page.tsx`:
   ```tsx
   import { Vault } from "lucide-react";
   import { PlaceholderPage } from "@/components/placeholder-page";
   export default function TreasuryPage() {
     return <PlaceholderPage icon={Vault} title="Treasury" />;
   }
   ```

   `packages/app/src/app/staking/page.tsx`:
   ```tsx
   import { TrendingUp } from "lucide-react";
   import { PlaceholderPage } from "@/components/placeholder-page";
   export default function StakingPage() {
     return <PlaceholderPage icon={TrendingUp} title="Staking" />;
   }
   ```

   `packages/app/src/app/delegations/page.tsx`:
   ```tsx
   import { Users } from "lucide-react";
   import { PlaceholderPage } from "@/components/placeholder-page";
   export default function DelegationsPage() {
     return <PlaceholderPage icon={Users} title="Delegations" />;
   }
   ```

   `packages/app/src/app/playground/page.tsx`:
   ```tsx
   import { Terminal } from "lucide-react";
   import { PlaceholderPage } from "@/components/placeholder-page";
   export default function PlaygroundPage() {
     return <PlaceholderPage icon={Terminal} title="Playground" />;
   }
   ```

4. Update `packages/app/src/app/page.tsx` to redirect to `/treasury` (default landing page):
   ```tsx
   import { redirect } from "next/navigation";
   export default function Home() {
     redirect("/treasury");
   }
   ```

5. Update `packages/app/src/app/layout.tsx`:
   - Import `Web3Provider` from `@/providers/web3-provider`
   - Import `AppProvider` from `@/providers/app-provider`
   - Import `Sidebar` from `@/components/sidebar`
   - Import `DemoBanner` from `@/components/demo-banner`
   - Wrap `{children}` in the provider stack: `<Web3Provider><AppProvider>...</AppProvider></Web3Provider>`
   - Layout structure inside providers:
     ```tsx
     <DemoBanner />
     <div className="flex min-h-screen">
       <Sidebar />
       <main className="flex-1 ml-14 p-8 max-w-[1200px] mx-auto transition-all duration-200">
         {children}
       </main>
     </div>
     ```
   - The `ml-14` (56px) matches collapsed sidebar width. Main content area does NOT need to shift on sidebar hover -- sidebar overlays content when expanded (position fixed handles this).
   - Keep the Inter font and dark mode class="dark" from Plan 01
  </action>
  <verify>
    <automated>cd packages/app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - packages/app/src/components/sidebar.tsx contains '"use client"'
    - packages/app/src/components/sidebar.tsx contains "Treasury"
    - packages/app/src/components/sidebar.tsx contains "Staking"
    - packages/app/src/components/sidebar.tsx contains "Delegations"
    - packages/app/src/components/sidebar.tsx contains "Playground"
    - packages/app/src/components/sidebar.tsx contains "usePathname"
    - packages/app/src/components/sidebar.tsx contains "w-14"
    - packages/app/src/components/sidebar.tsx contains "group-hover:w-60" or "group-hover:" with width
    - packages/app/src/components/sidebar.tsx contains "focus-within"
    - packages/app/src/components/sidebar.tsx contains 'aria-label="Main navigation"'
    - packages/app/src/components/sidebar.tsx contains "AgentGate"
    - packages/app/src/components/sidebar.tsx contains "AG"
    - packages/app/src/components/sidebar.tsx contains "WalletDisplay"
    - packages/app/src/components/sidebar.tsx contains "border-primary" (active indicator)
    - packages/app/src/components/placeholder-page.tsx contains "Coming soon"
    - packages/app/src/components/placeholder-page.tsx contains "backdrop-blur"
    - packages/app/src/app/treasury/page.tsx contains "Treasury"
    - packages/app/src/app/staking/page.tsx contains "Staking"
    - packages/app/src/app/delegations/page.tsx contains "Delegations"
    - packages/app/src/app/playground/page.tsx contains "Playground"
    - packages/app/src/app/page.tsx contains 'redirect("/treasury")'
    - packages/app/src/app/layout.tsx contains "Web3Provider"
    - packages/app/src/app/layout.tsx contains "AppProvider"
    - packages/app/src/app/layout.tsx contains "Sidebar"
    - packages/app/src/app/layout.tsx contains "DemoBanner"
    - `cd packages/app && npm run build` exits 0
  </acceptance_criteria>
  <done>Icon rail sidebar with 4 nav items (Treasury, Staking, Delegations, Playground) expands on hover from 56px to 240px with 200ms transition. Active route has accent left border and icon. AgentGate wordmark with glow at top, wallet section at bottom. All four section pages render centered glassmorphism placeholder cards with section icon and "Coming soon". Root page redirects to /treasury. Layout wires providers, sidebar, demo banner, and main content area.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete app shell</name>
  <what-built>Complete Phase 1 app shell with dark crypto theme, sidebar navigation, wallet connect, and demo mode</what-built>
  <how-to-verify>
    1. Run `npm run dev` from the monorepo root
    2. Open http://localhost:3000 in the browser
    3. Verify you are redirected to /treasury
    4. Verify the dark theme -- deep navy/charcoal background (#0B0F1A), not white or gray
    5. Verify the demo banner at top: "Demo Mode -- viewing 0x0000...0000" with "Connect Wallet" link
    6. Verify the sidebar:
       a. Shows icon rail (narrow, ~56px) with 4 icons
       b. Hover over sidebar -- it expands to show labels: Treasury, Staking, Delegations, Playground
       c. "AgentGate" wordmark visible when expanded, "AG" when collapsed
       d. Active nav item (Treasury) has purple/accent left border
       e. "Connect Wallet" button at bottom of sidebar
    7. Click each nav item -- verify navigation works and each shows a glassmorphism "Coming soon" card
    8. Click "Connect Wallet" -- RainbowKit modal should appear with dark theme
    9. (Optional) Connect MetaMask if available -- demo banner should disappear, address + Base badge should show in sidebar
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <action>Human visual verification of the complete app shell. Start dev server and walk through the verification steps above.</action>
  <verify>User confirms app shell matches design spec</verify>
  <done>User approves the visual appearance, navigation, wallet connect, and demo mode behavior</done>
</task>

</tasks>

<verification>
1. `cd packages/app && npm run build` -- full build passes
2. `npm run dev` starts dev server without errors
3. Browser at localhost:3000 redirects to /treasury
4. All 4 nav routes render placeholder pages
5. Sidebar expands on hover, collapses on mouse leave
6. Demo banner visible, disappears when wallet connected
7. RainbowKit connect modal opens with dark theme
8. CSS matches UI-SPEC (dark background, accent purple, glassmorphism cards)
</verification>

<success_criteria>
- Sidebar navigation with 4 items works (icon rail, hover expand, active state, focus-within)
- RainbowKit wallet connect with dark theme opens and functions
- Demo mode active by default, banner shows truncated treasury address
- Connected state shows ENS/address + Base badge, hides demo banner
- All placeholder pages render glassmorphism cards
- Root redirects to /treasury
- Full app builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
